export type Course = {
  id: string
  title: string
  subtitle: string
  description: string
  coverImageUrl: string
}

// --- Interface ---------------------------------------------------------------
export type TableHeaders = {
  title: string
  description: string
}
